{
  "name": "esy-glfw",
  "version": "3.2.1010",
  "description": "Esy GLFW build",
  "license": "MIT",
  "esy": {
    "build": [
	["make", "#{os == 'darwin' ? 'install' : 'noop'}", "PREFIX=#{self.install}"]
    ],
    "buildsInSource":true
  },
  "dependencies": {
    "esy-cmake": "^0.3.2"
  }
}
